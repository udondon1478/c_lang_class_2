#include <stdio.h>

#define MAX (20)
int main(int argc, char const *argv[])
{
    char str[MAX];
    int i;

    printf("暗号文:");
    scanf("%s", str);
    printf("暗号文:")

    for (int i = 0; str[i] != count; i++)
    {
        /* code */
    }
    

    return 0;
}
