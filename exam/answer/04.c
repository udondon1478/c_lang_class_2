#include <stdio.h>
#define NMAX(10)
void printArray(int *ptr, int n){
    int i;
    for (i = 0; i < n; i++)
    {
        printf("%d ", *ptr);
        ptr++;
    }
    printf("\n");
}
int main(int argc, char const *argv[])
{
    int i, n, a[NMAX];
    scanf("%d", &n);
    for (i = 0; i < n; i++)
    {
        
    }
    
    return 0;
}
